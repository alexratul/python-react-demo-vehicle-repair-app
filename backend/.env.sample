# Demo credentials
DEMO_USERNAME=admin
DEMO_PASSWORD=password


# JWT secret (change in real deployments)
JWT_SECRET=change-me
ACCESS_MINUTES=120


# DB: default is sqlite file inside the container volume
# For MySQL (example):
# DATABASE_URL=mysql+pymysql://user:pass@mysql:3306/estimates
DATABASE_URL=sqlite:///./estimates.db


# CORS
FRONTEND_ORIGIN=http://localhost:5173